import { ScrollEnteranceDirective } from './scroll-enterance.directive';

describe('ScrollEnteranceDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollEnteranceDirective();
    expect(directive).toBeTruthy();
  });
});
